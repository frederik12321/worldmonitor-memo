# WorldMonitor MCP Server configuration
# Copy to .env and fill in your values: cp .env.example .env

# WorldMonitor API base URL (default: https://worldmonitor.app)
WORLDMONITOR_BASE_URL=https://worldmonitor.app

# Optional API key if your deployment requires one
WORLDMONITOR_API_KEY=
